import { ReactNode } from "react";
import Modal from "../../modal/Modal";

const ProductRegistModal = (props: { children: ReactNode }) => {
  return (
    <Modal>
      <div></div>
    </Modal>
  );
};

export default ProductRegistModal;
